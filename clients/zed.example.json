{
  "_comment": "Zed Editor — merge into ~/.config/zed/settings.json. Zed's current schema is flat: command + args (the older nested command:{path,args} form is no longer accepted).",
  "_path_windows": "%APPDATA%\\Zed\\settings.json",
  "_path_macos": "~/.config/zed/settings.json",
  "context_servers": {
    "neuron5": {
      "command": "python.exe",
      "args": ["-m", "neuron"]
    }
  }
}
