# This directory ships the seed knowledge DB inside the wheel.
# Place base_knowledge.db here (regenerated via scripts/seed_vault.py).
# Loaded at runtime by neuron.registry.GraphRegistry via importlib.resources.
